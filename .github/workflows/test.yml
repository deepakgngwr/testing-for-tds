name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 22f1001001@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Done
        run: echo "Workflow completed"
